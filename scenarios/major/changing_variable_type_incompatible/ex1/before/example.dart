// Simple example demonstrating variables with types that will change
library example;

/// A variable with a general type
Object defaultValue = 'default';

/// A class with a typed field
class Config {
  /// A variable with a list type
  static List<dynamic> supportedTypes = ['string', 'number', 'boolean'];
}
