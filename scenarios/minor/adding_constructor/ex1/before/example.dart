// Simple example demonstrating a class with limited constructors
library example;

/// A class with only a default constructor
class User {
  final String name;
  final int age;

  /// Default constructor
  User(this.name, this.age);
}
